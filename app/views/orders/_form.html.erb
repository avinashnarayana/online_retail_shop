<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :delivery_name %>
    <%= f.text_field :delivery_name %>
  </div>

  <div class="field">
    <%= f.label :delivery_address %>
    <%= f.text_area :delivery_address %>
  </div>

  <div class="field">
    <%= f.label :order_updates %>
    <%= f.text_area :order_updates %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
