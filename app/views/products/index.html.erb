<% provide(:title, 'Products') %>
<h1>All Products</h1>
<%= if !!current_user && current_user.role == 1
        link_to 'Add New Product', new_product_path, class: "btn btn-primary btn-block"
end %>
<div class="container-fluid center", id="products-index">
  <%= render @products %>
  <%= will_paginate @products %>
</div>
</ul>